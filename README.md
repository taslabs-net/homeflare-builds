# homeflare-builds

Darwin arm64 builds of software whose vendors ship no macOS binary, published as
immutable, attested GitHub Releases for HomeFlare's `ReleaseBinary` to install.

The build definitions land in the first pull request. The build scripts are MIT
(see LICENSE); every binary keeps its own upstream licence, shipped inside its archive.
